export {
  buildTreeStyles,
  INDENT_PER_DEPTH,
  ROW_MIN_HEIGHT,
  type TreeStyles,
} from './tree-styles.util';
export {
  buildMatcher,
  clipMatches,
  DEFAULT_SEARCH_MODES,
  findMatches,
  MAX_SEARCHABLE_LENGTH,
  splitByMatches,
  testMatch,
  type Matcher,
  type MatchRange,
  type SearchModes,
  type SearchQuery,
  type TextSegment,
} from './text-search.util';
