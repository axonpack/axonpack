export type { PrettyPrintTheme } from './theme.const';

/**
 * All 130 palettes: the two neutral defaults, then 128 constructed from a base hue, a mode and a
 * character. The recipe, and the contrast rules every one of them holds to, are in the file.
 */
export * from './palettes.const';
